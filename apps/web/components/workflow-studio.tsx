"use client";

import { useEffect, useState } from "react";
import { useAuthenticationStatus, useNhostClient, useSignInEmailPassword, useSignOut, useUserData } from "@nhost/nextjs";
import { APPROVE_STEP_MUTATION, STEP_RUNS_SUBSCRIPTION, TRIGGER_WORKFLOW_MUTATION, WORKSPACE_QUERY } from "../lib/graphql";

type Status = "pending" | "running" | "completed" | "failed" | "paused" | "skipped";
type StepType = "llm_call" | "http_request" | "conditional_branch" | "approval_gate" | "db_write" | "notify";
type TriggerType = "manual" | "webhook" | "scheduled" | "database_event";
type Role = "owner" | "editor" | "viewer";
type Step = { id: string; position: number; type: StepType; config: Record<string, unknown>; status?: Status };
type Trigger = { id: string; type: TriggerType; config: Record<string, unknown>; enabled: boolean };
type Run = { id: string; status: Status; trigger_type: string; started_at: string; completed_at: string | null; error: Record<string, unknown> | null };
type Workflow = { id: string; name: string; description: string; active: boolean; workflow_steps: Step[]; workflow_triggers: Trigger[]; workflow_runs: Run[] };
type Workspace = { organizations_by_pk: { id: string; name: string; quota_used: number; quota_limit: number } | null; workflows: Workflow[] };
type StepRun = { id: string; workflow_run_id: string; workflow_step_id: string; status: Status; output: Record<string, unknown> | null; error: Record<string, unknown> | null; attempt_count: number; approved_by: string | null; approved_at: string | null; started_at: string | null; completed_at: string | null };

const ORG_QUERY = `query Organizations { org_members { org_id role organization { id name quota_used quota_limit } } }`;
const SAVE_WORKFLOW_MUTATION = `mutation SaveWorkflow($workflow: jsonb!) { saveWorkflow(workflow: $workflow) { workflow_id } }`;
const typeMeta: Record<StepType, { icon: string; label: string; tone: string }> = { llm_call: { icon: "✦", label: "LLM call", tone: "mint" }, http_request: { icon: "↗", label: "HTTP request", tone: "blue" }, conditional_branch: { icon: "◇", label: "Conditional branch", tone: "violet" }, approval_gate: { icon: "⌁", label: "Approval gate", tone: "coral" }, db_write: { icon: "▣", label: "Database write", tone: "blue" }, notify: { icon: "◌", label: "Notify", tone: "violet" } };
const baseConfig: Record<StepType, Record<string, unknown>> = {
  llm_call: { title: "Classify input", prompt: "Classify this input as approve or review: {{triggerInput}}", model: "llama-3.1-8b-instant" },
  http_request: { title: "Fetch context", method: "GET", url: "https://httpbin.org/json" },
  conditional_branch: { title: "Choose a lane", expression: "contains approve", whenTrue: "fast-lane", whenFalse: "manual-review" },
  approval_gate: { title: "Human review", reason: "A teammate must confirm this run." },
  db_write: { title: "Store result", target: "workflow_results" },
  notify: { title: "Notify team", destination: "configured destination" },
};
const messageOf = (value: unknown) => Array.isArray(value) ? value.map((item) => String((item as { message?: string }).message ?? item)).join(", ") : String((value as { message?: string } | null)?.message ?? value ?? "Unknown error");
const localId = () => `new-${Math.random().toString(36).slice(2)}`;
const formatTime = (value: string) => new Date(value).toLocaleString();
const duration = (run: Run) => run.completed_at ? `${Math.max(0, Math.round((new Date(run.completed_at).getTime() - new Date(run.started_at).getTime()) / 1000))}s` : "—";

function SignIn() {
  const { signInEmailPassword, isLoading, error } = useSignInEmailPassword(); const [email, setEmail] = useState(""); const [password, setPassword] = useState("");
  return <main className="auth-shell"><div className="auth-card"><div className="brand auth-brand"><div className="brand-mark"><span /><span /><span /></div><span>relay<span className="brand-soft">room</span></span></div><span className="section-kicker">SECURE WORKSPACE</span><h1>Sign in to your control room.</h1><p>Use your Relay Room account to access organization-scoped workflows and run history.</p><form onSubmit={(event) => { event.preventDefault(); void signInEmailPassword(email, password); }}><label>Email<input type="email" value={email} onChange={(event) => setEmail(event.target.value)} required /></label><label>Password<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} required /></label>{error && <div className="inline-error">{messageOf(error)}</div>}<button className="run-button auth-submit" disabled={isLoading}>{isLoading ? "Signing in…" : "Sign in"}</button></form></div></main>;
}

function StatusPill({ status }: { status: Status }) { return <span className={`status status-${status}`}><span className="status-dot" />{status === "paused" ? "Awaiting approval" : status}</span>; }

function StepFields({ step, onChange, readOnly }: { step: Step; onChange: (config: Record<string, unknown>) => void; readOnly: boolean }) {
  const update = (key: string, value: string) => onChange({ ...step.config, [key]: value });
  const input = (label: string, key: string, multiline = false) => <label className="field-label">{label}{multiline ? <textarea className="code-field" disabled={readOnly} value={String(step.config[key] ?? "")} onChange={(e) => update(key, e.target.value)} /> : <input className="text-field" disabled={readOnly} value={String(step.config[key] ?? "")} onChange={(e) => update(key, e.target.value)} />}</label>;
  if (step.type === "llm_call") return <>{input("Title", "title")}{input("Prompt", "prompt", true)}{input("Model", "model")}</>;
  if (step.type === "http_request") return <>{input("Title", "title")}<label className="field-label">Method<select className="text-field" disabled={readOnly} value={String(step.config.method ?? "GET")} onChange={(e) => update("method", e.target.value)}><option>GET</option><option>POST</option><option>PUT</option><option>PATCH</option><option>DELETE</option></select></label>{input("HTTPS URL", "url")}{input("JSON body (optional)", "body", true)}</>;
  if (step.type === "conditional_branch") return <>{input("Title", "title")}{input("Condition", "expression")}<p className="field-help">Example: <code>contains approve</code>. It is evaluated against the previous step output.</p>{input("True outcome", "whenTrue")}{input("False outcome", "whenFalse")}</>;
  if (step.type === "approval_gate") return <>{input("Title", "title")}{input("Approval message", "reason", true)}</>;
  if (step.type === "db_write") return <>{input("Title", "title")}<p className="field-help">Results are written only to <code>workflow_results</code>.</p></>;
  return <>{input("Title", "title")}{input("Destination", "destination")}</>;
}

function useWorkspace() {
  const nhost = useNhostClient(); const { isAuthenticated, isLoading: authLoading } = useAuthenticationStatus(); const user = useUserData();
  const [orgId, setOrgId] = useState<string>(); const [role, setRole] = useState<Role>(); const [memberships, setMemberships] = useState<Array<{ org_id: string; role: Role; organization: { id: string; name: string } }>>([]); const [data, setData] = useState<Workspace>(); const [error, setError] = useState<string>(); const [loading, setLoading] = useState(true);
  const load = async (requestedOrg = orgId) => { if (!isAuthenticated) return; try { setError(undefined); const membershipResult = await nhost.graphql.request<{ org_members: Array<{ org_id: string; role: Role; organization: { id: string; name: string } }> }>(ORG_QUERY); if (membershipResult.error) throw new Error(messageOf(membershipResult.error)); const available = membershipResult.data?.org_members ?? []; setMemberships(available); const member = available.find((item) => item.org_id === requestedOrg) ?? available[0]; if (!member) throw new Error("No organization membership found for this account."); setOrgId(member.org_id); setRole(member.role); const result = await nhost.graphql.request<Workspace>(WORKSPACE_QUERY, { orgId: member.org_id }); if (result.error) throw new Error(messageOf(result.error)); setData(result.data ?? undefined); } catch (caught) { setError(caught instanceof Error ? caught.message : "Unable to load workspace"); } finally { setLoading(false); } };
  useEffect(() => { if (isAuthenticated) void load(); }, [isAuthenticated]);
  return { nhost, user, isAuthenticated, authLoading, loading, error, data, role, orgId, memberships, reload: load };
}

export default function WorkflowStudio() {
  const live = useWorkspace(); const { signOut } = useSignOut();
  const [view, setView] = useState<"builder" | "runs">("builder"); const [selectedWorkflowId, setSelectedWorkflowId] = useState<string>(); const [selectedStepId, setSelectedStepId] = useState<string>(); const [selectedRunId, setSelectedRunId] = useState<string>(); const [draft, setDraft] = useState<Workflow>(); const [stepRuns, setStepRuns] = useState<StepRun[]>([]); const [subscription, setSubscription] = useState<"connecting" | "live" | "disconnected">("disconnected"); const [toast, setToast] = useState(""); const [runError, setRunError] = useState(""); const [saveError, setSaveError] = useState(""); const [approveError, setApproveError] = useState(""); const [running, setRunning] = useState(false); const [saving, setSaving] = useState(false); const [approving, setApproving] = useState(false);
  const workflows = live.data?.workflows ?? [];
  useEffect(() => { if (!workflows.length) { setSelectedWorkflowId(undefined); setDraft(undefined); return; } if (!selectedWorkflowId || !workflows.some((workflow) => workflow.id === selectedWorkflowId)) setSelectedWorkflowId(workflows.at(0)?.id); }, [workflows, selectedWorkflowId]);
  const serverWorkflow = workflows.find((workflow) => workflow.id === selectedWorkflowId);
  const workflow = draft ?? serverWorkflow;
  useEffect(() => { setDraft(undefined); setSelectedStepId(undefined); setSelectedRunId(serverWorkflow?.workflow_runs[0]?.id); }, [serverWorkflow?.id]);
  const memberRole = live.role ?? "viewer"; const canEdit = memberRole !== "viewer"; const isOwner = memberRole === "owner";
  const selectedRun = workflow?.workflow_runs.find((run) => run.id === selectedRunId) ?? workflow?.workflow_runs[0];
  useEffect(() => { if (!selectedRun || !live.isAuthenticated) { setStepRuns([]); return; } let cancelled = false; const loadStepRuns = async () => { const result = await live.nhost.graphql.request<{ step_runs: StepRun[] }>(STEP_RUNS_SUBSCRIPTION.replace("subscription StepRuns", "query StepRuns"), { runId: selectedRun.id }); if (!cancelled && result.data) setStepRuns(result.data.step_runs); }; void loadStepRuns(); return () => { cancelled = true; }; }, [selectedRun?.id, live.isAuthenticated]);
  useEffect(() => { const runId = selectedRun?.id; const wsUrl = process.env.NEXT_PUBLIC_GRAPHQL_WS_URL ?? process.env.NEXT_PUBLIC_GRAPHQL_URL?.replace(/^http/, "ws"); if (!live.isAuthenticated || !runId || !wsUrl || typeof WebSocket === "undefined") { setSubscription("disconnected"); return; } let closed = false; setSubscription("connecting"); const socket = new WebSocket(wsUrl, "graphql-transport-ws"); const start = async () => { const tokenValue = (live.nhost.auth as unknown as { getAccessToken?: () => string | Promise<string | null> | null }).getAccessToken?.(); const token = await Promise.resolve(tokenValue); socket.send(JSON.stringify({ type: "connection_init", payload: token ? { headers: { Authorization: `Bearer ${token}` } } : {} })); };
    socket.onopen = () => { void start().catch(() => { setSubscription("disconnected"); socket.close(); }); };
    socket.onmessage = (event) => { const message = JSON.parse(String(event.data)) as { type?: string; payload?: { data?: { step_runs?: StepRun[] } } }; if (message.type === "connection_ack") { socket.send(JSON.stringify({ id: "step-runs", type: "subscribe", payload: { query: STEP_RUNS_SUBSCRIPTION, variables: { runId } } })); setSubscription("live"); } if (message.type === "next" && message.payload?.data?.step_runs) setStepRuns(message.payload.data.step_runs); if (message.type === "error") setSubscription("disconnected"); };
    socket.onerror = () => setSubscription("disconnected"); socket.onclose = () => { if (!closed) setSubscription("disconnected"); }; return () => { closed = true; socket.close(); };
  }, [selectedRun?.id, live.isAuthenticated, live.nhost.auth]);
  useEffect(() => { if (subscription !== "disconnected" || !selectedRun) return; const timer = window.setInterval(() => void live.reload(), 5000); return () => window.clearInterval(timer); }, [subscription, selectedRun?.id]);
  const steps = workflow?.workflow_steps ?? []; const selectedStep = steps.find((step) => step.id === selectedStepId) ?? steps[0];
  const stepStatus = (step: Step): Status => stepRuns.find((run) => run.workflow_step_id === step.id)?.status ?? "pending";
  const notify = (message: string) => { setToast(message); window.setTimeout(() => setToast(""), 3000); };
  const updateWorkflow = (change: (current: Workflow) => Workflow) => { if (workflow) setDraft(change(workflow)); };
  const updateStep = (id: string, config: Record<string, unknown>) => updateWorkflow((current) => ({ ...current, workflow_steps: current.workflow_steps.map((step) => step.id === id ? { ...step, config } : step) }));
  const addStep = (type: StepType) => updateWorkflow((current) => ({ ...current, workflow_steps: [...current.workflow_steps, { id: localId(), position: current.workflow_steps.length, type, config: baseConfig[type] }] }));
  const removeStep = (id: string) => updateWorkflow((current) => ({ ...current, workflow_steps: current.workflow_steps.filter((step) => step.id !== id).map((step, position) => ({ ...step, position })) }));
  const moveStep = (id: string, direction: -1 | 1) => updateWorkflow((current) => { const index = current.workflow_steps.findIndex((step) => step.id === id); const next = index + direction; if (index < 0 || next < 0 || next >= current.workflow_steps.length) return current; const reordered = [...current.workflow_steps]; [reordered[index], reordered[next]] = [reordered[next], reordered[index]]; return { ...current, workflow_steps: reordered.map((step, position) => ({ ...step, position })) }; });
  const addWorkflow = () => { if (!live.orgId || !canEdit) return; const next: Workflow = { id: localId(), name: "Untitled workflow", description: "Describe what this workflow does.", active: true, workflow_steps: [], workflow_triggers: [{ id: localId(), type: "manual", config: {}, enabled: true }], workflow_runs: [] }; setSelectedWorkflowId(next.id); setDraft(next); setSelectedStepId(undefined); setSelectedRunId(undefined); setView("builder"); };
  const save = async () => { if (!workflow || !canEdit || saving || !live.orgId) return; setSaving(true); setSaveError(""); try { const result = await live.nhost.graphql.request(SAVE_WORKFLOW_MUTATION, { workflow: { workflow_id: workflow.id.startsWith("new-") ? undefined : workflow.id, organization_id: live.orgId, name: workflow.name, description: workflow.description, active: workflow.active, steps: workflow.workflow_steps.map((step, position) => ({ position, type: step.type, config: step.config })), triggers: workflow.workflow_triggers.map((trigger) => ({ type: trigger.type, config: trigger.config, enabled: trigger.enabled })) } }); if (result.error) throw new Error(messageOf(result.error)); const id = result.data?.saveWorkflow.workflow_id; await live.reload(); setDraft(undefined); if (id) setSelectedWorkflowId(id); notify("Workflow saved"); } catch (error) { setSaveError(messageOf(error)); } finally { setSaving(false); } };
  const runWorkflow = async () => { if (!workflow || !canEdit || running) return; setRunning(true); setRunError(""); try { const result = await live.nhost.graphql.request(TRIGGER_WORKFLOW_MUTATION, { workflowId: workflow.id, context: { source: "manual" } }); if (result.error) throw new Error(messageOf(result.error)); setSelectedRunId(result.data?.triggerWorkflowRun.run_id); notify("Workflow started"); await live.reload(); } catch (error) { setRunError(messageOf(error)); } finally { setRunning(false); } };
  const approve = async () => { const gate = stepRuns.find((run) => run.status === "paused"); if (!canEdit || !selectedRun || !gate || approving) return; setApproving(true); setApproveError(""); try { const result = await live.nhost.graphql.request(APPROVE_STEP_MUTATION, { runId: selectedRun.id, stepRunId: gate.id, decision: "approve" }); if (result.error) throw new Error(messageOf(result.error)); notify("Approved — workflow resumed"); await live.reload(); } catch (error) { setApproveError(messageOf(error)); } finally { setApproving(false); } };
  if (live.authLoading) return <main className="auth-shell"><div className="auth-card"><h1>Connecting to your workspace…</h1></div></main>;
  if (!live.isAuthenticated) return <SignIn />;
  if (live.loading) return <main className="auth-shell"><div className="auth-card"><h1>Loading organization data…</h1></div></main>;
  if (live.error || !live.data?.organizations_by_pk) return <main className="auth-shell"><div className="auth-card"><span className="section-kicker">WORKSPACE UNAVAILABLE</span><h1>{live.error ?? "Organization data is unavailable."}</h1><button className="run-button" onClick={() => void live.reload()}>Try again</button></div></main>;
  const organization = live.data.organizations_by_pk;
  if (!workflow) return <main className="app-shell"><aside className="sidebar"><div className="brand"><span>relay<span className="brand-soft">room</span></span></div></aside><section className="workspace"><div className="content empty-state"><span className="section-kicker">NO WORKFLOWS</span><h1>No workflow yet</h1><p>{canEdit ? "Create a workflow to begin building an execution path." : "Read-only access — ask an owner or editor to create a workflow."}</p>{canEdit && <button className="run-button" onClick={addWorkflow}>Create workflow</button>}</div></section></main>;
  const progress = steps.length ? Math.round((steps.filter((step) => stepStatus(step) === "completed").length / steps.length) * 100) : 0;
  const availableTypes = (Object.keys(typeMeta) as StepType[]).filter((type) => isOwner || (type !== "db_write" && type !== "notify"));
  return <main className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark"><span /><span /><span /></div><span>relay<span className="brand-soft">room</span></span></div><div className="workspace-label">Workspace</div>{live.memberships.length > 1 ? <label className="org-switcher"><span className="org-avatar">{organization.name[0]}</span><select value={live.orgId} onChange={(event) => void live.reload(event.target.value)} aria-label="Select organization">{live.memberships.map((item) => <option key={item.org_id} value={item.org_id}>{item.organization.name} · {item.role}</option>)}</select></label> : <div className="org-switcher"><span className="org-avatar">{organization.name[0]}</span><span><b>{organization.name}</b><small>{memberRole}</small></span></div>}<nav className="nav"><button className={view === "builder" ? "nav-item active" : "nav-item"} onClick={() => setView("builder")}>⌘ Workflows <span className="nav-count">{workflows.length}</span></button><button className={view === "runs" ? "nav-item active" : "nav-item"} onClick={() => setView("runs")}>◷ Run history</button></nav><div className="sidebar-bottom"><div className="quota-mini"><div><span>Monthly usage</span><b>{organization.quota_used} <em>/ {organization.quota_limit} runs</em></b></div><div className="mini-bar"><span style={{ width: `${Math.min(100, organization.quota_used / organization.quota_limit * 100)}%` }} /></div><small>{Math.max(0, organization.quota_limit - organization.quota_used)} runs remaining</small></div><div className="profile"><span>{live.user?.displayName ?? live.user?.email}</span><button className="more" aria-label="Sign out" onClick={() => void signOut()}>↪</button></div></div></aside><section className="workspace"><header className="topbar"><div className="breadcrumbs"><span>Workflows</span><i>/</i><b>{workflow.name}</b></div><div className="top-actions"><span className="live-dot"><i />{subscription === "live" ? "Live updates" : subscription === "connecting" ? "Connecting…" : "Refresh fallback"}</span>{canEdit && <button className="run-button" disabled={running || workflow.id.startsWith("new-")} onClick={() => void runWorkflow()}>{running ? "Starting…" : "Run workflow"}</button>}</div></header><div className="content"><div className="workflow-heading"><div><div className="eyebrow">{workflow.active ? "ACTIVE WORKFLOW" : "DRAFT WORKFLOW"} · {memberRole.toUpperCase()} ACCESS</div>{canEdit ? <><input className="workflow-title-input" value={workflow.name} onChange={(event) => updateWorkflow((current) => ({ ...current, name: event.target.value }))} aria-label="Workflow name" /><textarea className="workflow-description-input" value={workflow.description} onChange={(event) => updateWorkflow((current) => ({ ...current, description: event.target.value }))} aria-label="Workflow description" /></> : <><h1>{workflow.name}</h1><p>{workflow.description}</p><span className="readonly-badge">Read-only access</span></>}</div><div className="heading-actions"><label className="workflow-picker">Workflow<select value={workflow.id} onChange={(event) => { setDraft(undefined); setSelectedWorkflowId(event.target.value); }}>{workflows.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label>{canEdit && <><button className="secondary-button" onClick={addWorkflow}>New workflow</button><button className="secondary-button" disabled={saving} onClick={() => void save()}>{saving ? "Saving…" : "Save changes"}</button></>}</div></div>{saveError && <div className="inline-error">{saveError}</div>}{runError && <div className="inline-error">{runError}</div>}<div className="tabs"><button className={view === "builder" ? "tab active" : "tab"} onClick={() => setView("builder")}>Builder <span>{steps.length}</span></button><button className={view === "runs" ? "tab active" : "tab"} onClick={() => setView("runs")}>Runs <span>{workflow.workflow_runs.length}</span></button><span className="autosave"><i />{subscription === "live" ? "Live from Nhost" : subscription === "connecting" ? "Connecting to updates" : "Updates refresh every 5s"}</span></div>{view === "builder" ? <><div className="signal-card"><div className="signal-card-copy"><div className="eyebrow mint-eyebrow">{selectedRun ? `RUN DETAILS · ${selectedRun.id.slice(0, 8).toUpperCase()}` : "READY TO RUN"}</div><h2>{selectedRun?.status === "paused" ? "Waiting for approval" : selectedRun?.status === "completed" ? "Run completed" : selectedRun ? "Workflow run" : "Build an execution path"}</h2><p>{selectedRun ? `${selectedRun.trigger_type} · started ${formatTime(selectedRun.started_at)}` : "Add ordered steps and save before running."}</p><div className="run-meta"><StatusPill status={selectedRun?.status ?? "pending"} /></div></div><div className="signal-orbit"><div className="orbit-core"><b>{progress}</b><small>% complete</small></div></div></div><div className="builder-grid"><section className="steps-panel"><div className="section-head"><div><span className="section-kicker">EXECUTION PATH</span><h2>Workflow steps <span>{steps.length}</span></h2></div>{canEdit && <label className="small-link">Add step<select className="inline-select" value="" onChange={(event) => { if (event.target.value) { addStep(event.target.value as StepType); event.target.value = ""; } }}><option value="">Choose type</option>{availableTypes.map((type) => <option key={type} value={type}>{typeMeta[type].label}</option>)}</select></label>}</div><div className="step-list">{steps.length ? steps.map((step, index) => <button className={`step-row ${selectedStep?.id === step.id ? "selected" : ""}`} key={step.id} onClick={() => setSelectedStepId(step.id)}><div className={`step-marker ${typeMeta[step.type].tone}`}><span>{typeMeta[step.type].icon}</span>{index < steps.length - 1 && <i />}</div><div className="step-main"><div className="step-topline"><span className="step-eyebrow">{String(index + 1).padStart(2, "0")} · {typeMeta[step.type].label}</span><StatusPill status={stepStatus(step)} /></div><h3>{String(step.config.title ?? typeMeta[step.type].label)}</h3><p>{step.type === "http_request" ? `${String(step.config.method ?? "GET")} · ${String(step.config.url ?? "")}` : String(step.config.reason ?? step.config.expression ?? step.config.prompt ?? "Configured workflow step")}</p></div></button>) : <p className="empty-copy">No steps yet. Add the first step to define this workflow.</p>}</div></section><aside className="inspector"><div className="inspector-top"><div><span className="section-kicker">{selectedStep ? `STEP ${String(selectedStep.position + 1).padStart(2, "0")}` : "WORKFLOW"}</span><h2>{selectedStep ? typeMeta[selectedStep.type].label : "Select a step"}</h2></div></div>{selectedStep ? <><StepFields step={selectedStep} readOnly={!canEdit} onChange={(config) => updateStep(selectedStep.id, config)} />{selectedStep.type === "approval_gate" && <div className="approval-box"><div><b>Approval required</b><p>Owners and editors in {organization.name} can approve this gate.</p></div></div>}{selectedStep.status !== "paused" && stepRuns.find((run) => run.workflow_step_id === selectedStep.id)?.output && <details className="raw-output"><summary>Run output</summary><pre>{JSON.stringify(stepRuns.find((run) => run.workflow_step_id === selectedStep.id)?.output, null, 2)}</pre></details>}{canEdit && <div className="inspector-footer"><button className="ghost-button" disabled={selectedStep.position === 0} onClick={() => moveStep(selectedStep.id, -1)}>Move up</button><button className="ghost-button" disabled={selectedStep.position === steps.length - 1} onClick={() => moveStep(selectedStep.id, 1)}>Move down</button><button className="ghost-button danger" onClick={() => removeStep(selectedStep.id)}>Remove</button></div>}{stepStatus(selectedStep) === "paused" && canEdit && <><button className="approve-button" disabled={approving} onClick={() => void approve()}>{approving ? "Approving…" : "Approve & continue →"}</button>{approveError && <div className="inline-error">{approveError}</div>}</>}</> : <p className="empty-copy">Choose a step to configure it.</p>}</aside></div><div className="bottom-grid"><section className="trigger-section"><div className="section-head"><div><span className="section-kicker">STARTING POINTS</span><h2>Triggers <span>{workflow.workflow_triggers.length}</span></h2></div>{canEdit && <button className="small-link" onClick={() => updateWorkflow((current) => ({ ...current, workflow_triggers: [...current.workflow_triggers, { id: localId(), type: "manual", config: {}, enabled: true }] }))}>Add trigger</button>}</div><div className="trigger-list">{workflow.workflow_triggers.map((trigger, index) => <div key={trigger.id}><span>{trigger.type}</span>{canEdit ? <><select className="inline-select" value={trigger.type} onChange={(event) => { const type = event.target.value as TriggerType; if (type === "webhook" && !isOwner) return; updateWorkflow((current) => ({ ...current, workflow_triggers: current.workflow_triggers.map((item, itemIndex) => itemIndex === index ? { ...item, type, config: type === "webhook" ? { publicId: "" } : type === "scheduled" ? { cron: "0 * * * *" } : item.config } : item) })); }}><option value="manual">manual</option>{isOwner && <option value="webhook">webhook</option>}<option value="scheduled">scheduled</option><option value="database_event">database event</option></select>{trigger.type === "webhook" && <input className="text-field" placeholder="Public webhook ID" value={String(trigger.config.publicId ?? "")} onChange={(event) => updateWorkflow((current) => ({ ...current, workflow_triggers: current.workflow_triggers.map((item, itemIndex) => itemIndex === index ? { ...item, config: { ...item.config, publicId: event.target.value } } : item) }))} />}{trigger.type === "scheduled" && <input className="text-field" value={String(trigger.config.cron ?? "")} onChange={(event) => updateWorkflow((current) => ({ ...current, workflow_triggers: current.workflow_triggers.map((item, itemIndex) => itemIndex === index ? { ...item, config: { ...item.config, cron: event.target.value } } : item) }))} />}{trigger.type === "database_event" && <small>Starts from the watched_orders event trigger.</small>}<button className="ghost-button danger" onClick={() => updateWorkflow((current) => ({ ...current, workflow_triggers: current.workflow_triggers.filter((_, itemIndex) => itemIndex !== index) }))}>Remove</button></> : <small>{trigger.type === "webhook" ? `Signed endpoint public ID: ${String(trigger.config.publicId ?? "not configured")}` : JSON.stringify(trigger.config)}</small>}<em className="enabled">{trigger.enabled ? "Enabled" : "Disabled"}</em></div>)}</div>{workflow.workflow_triggers.some((trigger) => trigger.type === "webhook") && <p className="field-help">Webhook runs originate from the signed external <code>/webhookStartWorkflow</code> endpoint; this UI does not simulate them.</p>}</section><section className="activity-section"><div className="section-head"><span className="section-kicker">OBSERVABILITY</span><h2>Selected run</h2></div>{selectedRun ? <div className="activity-list">{stepRuns.length ? stepRuns.map((run) => <div className="activity-item" key={run.id}><span className={`activity-line ${run.status}`} /><time>{run.attempt_count}×</time><div><b>{steps.find((step) => step.id === run.workflow_step_id)?.config.title as string ?? "Workflow step"}</b><p>{run.error ? JSON.stringify(run.error) : run.status}</p></div></div>) : <p className="empty-copy">No step runs for this execution.</p>}</div> : <p className="empty-copy">Select or start a run to inspect step status.</p>}</section></div></> : <div className="runs-view"><div className="runs-summary"><div><span className="section-kicker">RUN HISTORY</span><h2>Recent workflow runs</h2><p>Past executions stay scoped to {organization.name}.</p></div></div>{workflow.workflow_runs.length ? workflow.workflow_runs.map((run) => <div className="run-row" key={run.id}><span className={`run-status-dot ${run.status}`} /><b>{run.id.slice(0, 8).toUpperCase()}</b><span>{run.trigger_type}</span><StatusPill status={run.status} /><small>{formatTime(run.started_at)} · {duration(run)}</small><button onClick={() => { setSelectedRunId(run.id); setView("builder"); }}>Inspect →</button></div>) : <p className="empty-copy">No runs yet. Run this workflow to create the first execution.</p>}</div>}</div></section>{toast && <div className="toast"><span>✓</span>{toast}</div>}</main>;
}
