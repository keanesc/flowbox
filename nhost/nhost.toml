# Nhost service configuration. Secret values intentionally stay in the ignored
# root .secrets file and in the Nhost dashboard; never add them here.
[global]

[[global.environment]]
name = 'GROQ_API_KEY'
value = '{{ secrets.GROQ_API_KEY }}'

[[global.environment]]
name = 'WEBHOOK_SIGNING_SECRET'
value = '{{ secrets.WEBHOOK_SIGNING_SECRET }}'

[[global.environment]]
name = 'RELAY_ACTION_SECRET'
value = '{{ secrets.RELAY_ACTION_SECRET }}'

[[global.environment]]
name = 'WORKFLOW_LLM_STUB'
value = 'false'

[hasura]
version = 'v2.48.10-ce'
adminSecret = '{{ secrets.HASURA_GRAPHQL_ADMIN_SECRET }}'
webhookSecret = '{{ secrets.NHOST_WEBHOOK_SECRET }}'

[[hasura.jwtSecrets]]
type = 'RS256'
key = '{{ secrets.NHOST_JWT_PUBLIC_KEY }}'
signingKey = '{{ secrets.NHOST_JWT_PRIVATE_KEY }}'
kid = '{{ secrets.NHOST_JWT_KID }}'

[functions]
[functions.node]
version = 22

[postgres]
version = '14.20-20251217-1'

[postgres.resources]
[postgres.resources.storage]
capacity = 1

[observability]
[observability.grafana]
adminPassword = '{{ secrets.GRAFANA_ADMIN_PASSWORD }}'
